export const AddToPortfolio = async (TickerSymbol: string) => {
	console.log(`Adding ${TickerSymbol} to portfolio`);
};

export const RemoveFromPortfolio = async (TickerSymbol: string) => {
	console.log(`Removing ${TickerSymbol} from portfolio`);
};
